{
  "name": "bbhegde-hrms-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Internal Android WebView shell for BB Hegde HRMS",
  "scripts": {
    "sync": "cap sync android",
    "android": "cap open android",
    "build:apk": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
